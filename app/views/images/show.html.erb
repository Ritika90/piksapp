<p>
  <strong>Name:</strong>
  <%= @image.name %>
</p>

<p>
  <%= image_tag @image.picture.url(:thumb) %>
</p>
<%= link_to 'Edit',  edit_album_image_path(@image.album, @image)%> |
<%= link_to 'Destroy', album_image_path, method: :delete, data: { confirm: 'Are you sure?' } %> |
<%= link_to 'Back', album_path(@image.album) %>
